/**
 * MyGraphLeafWithArgs class, representing a leaf in the scene graph.
 * @constructor
**/

function MyGraphLeafWithArgs(scene,nodeID,type,args)
{
	this.scene = scene;
	this.id = nodeID;
	this.type = type;
	this.args = args;
}

